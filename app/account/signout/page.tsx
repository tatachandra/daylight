'use client';
import {useState} from 'react';
import BrandMark from '@/app/brand-mark';
export default function SignOut(){const [message,setMessage]=useState(''),[busy,setBusy]=useState(false);async function signOut(){setBusy(true);setMessage('');try{const r=await fetch('/api/account/sign-out',{method:'POST',headers:{'Content-Type':'application/json'},body:'{}'});if(!r.ok)throw Error();location.assign('/wellness');}catch{setMessage('Could not sign out. Check your connection and try again.');setBusy(false);}}return <div className="site-shell account-shell"><header className="masthead"><a href="/" className="site-brand"><BrandMark/><span>My Day Harbor</span></a></header><main className="account-card" style={{maxWidth:480,margin:'40px auto'}}><h1>Ready to sign out?</h1><p>Your saved profile, meals and workouts will be here when you return.</p><button className="signin-button" disabled={busy} onClick={()=>void signOut()}>{busy?'Signing out…':'Sign out of your account'}</button><p><a href="/wellness">Stay signed in</a></p><p role="status">{message}</p></main></div>}
